# 📄 Documentación: `actuaciones_utils.py`

Módulo de funciones auxiliares utilizadas por `actuaciones.py` y otros componentes.

## 📚 Índice de funciones
- [limpiar_texto](#limpiar_texto)
- [normalizar_fecha](#normalizar_fecha)
- [generar_hash_archivo](#generar_hash_archivo)

### `limpiar_texto(texto)` → *retorna* `str`
> Limpia/normaliza cadenas de texto.

---

### `normalizar_fecha(texto)` → *retorna* `str`
> Normaliza o convierte fechas.

---

### `generar_hash_archivo(fecha, tipo, detalle, longitud=6)` → *retorna* `str`
> Genera o compara hashes/identificadores.

---
